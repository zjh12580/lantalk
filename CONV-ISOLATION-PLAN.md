# 会话隔离改造方案（阶段 3 剩余项）

## 一、要解决的问题

`messages` 表是**全员共享**的，任何人都能 `select * where id > lastId` 拉到所有人的消息
（包括陌生人之间的私聊正文）。当前唯一的防线在 `onNew()` 里：

```js
function onNew(m) {
  var c = cm()[m.conv];
  if (!c) { buildConvs(); c = cm()[m.conv]; }
  if (!c) return;   // ← 唯一的隔离判断
  ...
}
```

这有两个问题，一个是安全的，一个是架构的。

### 问题 1：隔离依赖「前端能推导出会话列表」，而推导源跟页面耦合

`buildConvs()` 推导私聊会话的输入是 `S.friends` + `S.recent`：

```js
var peers = {};
S.friends.forEach(...);          // 好友关系
(S.recent || []).forEach(...);   // ← 最近消息
```

也就是说：**一条消息能不能被认作「我的」，取决于它自己有没有先落进 `S.recent`。**
`S.recent` 由 `trackRecent()` 维护，而 `trackRecent` 从 `refreshAll` / `tick` / `sendMsg` 三处写入。
这个环是自洽的（能跑通），但它意味着隔离判定被挂在「消息缓存」上——
任何让 `S.recent` 与消息流不同步的改动，都会同时表现为「功能坏了」或「隔离漏了」，
而且**两者在测试里的表现是一样的**（消息不出现）。

### 问题 2：`m.conv` 到「我是否该看」的判断，没有任何地方显式表达

现在这个判断隐含在「`cm()` 能不能查到」里。`cm()` 查的是 `S.convs`，
而 `S.convs` 是**渲染用**的数据结构（含 `last` 预览、`muted`、`unread` 等展示字段）。

> 把「安全边界」和「渲染列表」用同一个对象表达，是这次要拆开的根本原因。
> 渲染列表可以因为 UI 需求随时增删字段、改变推导顺序；安全边界不行。

---

## 二、改造方案

### 核心思路：把隔离判定从「渲染列表」里提出来，变成一条独立、可测、有单一权威的谓词

新增 `canReadConv(conv)`，作为**唯一的**读权限判断：

```js
// 我参与的会话白名单。随数据同步重建，是「能否读某会话」的唯一权威。
// ⚠️ 安全边界：不要用 S.convs / cm() 代替它——那是渲染列表，
//    将来加「按名字过滤」「置顶折叠」之类的需求时很容易被改坏。
function myConvSet() { ... }        // 返回 { conv: 1 }
function canReadConv(conv) { ... }
```

白名单由**关系数据**推出，不再依赖消息：

| 会话类型 | 判定依据 | 来源 |
|---|---|---|
| 群聊 `g:<gid>` | 我在 `group_members` 里 / 是大厅 | `S.members`、`S.groups` |
| 私聊 `p:a~b` | 其中一方是我，且这段关系存在 | `S.friends`、`S.recent` |
| 本地导入 | 我主动导入的历史记录 | `S.localConvs` |
| 小美私聊 | 固定属于我 | `BOT.id` |

**关键约束（上次踩的坑）**：白名单绝不能只看 `S.friends` + `S.recent`，
否则「刚建好、还没消息、关系数据还没同步」的会话会不在白名单里，
它自己的第一条消息就永远进不来 —— 这就是上次 379 → 149 的原因。

三条兜底必须都在：

1. **关系数据兜底**：`S.friends` / `S.members` / `S.groups` 任一能推出 → 放行
2. **发送即注册**：自己 `sendMsg` 时白名单立即补上该会话，不等下一次同步
3. **成员资格兜底**：`group_members` 拉回来时白名单同步重建

### 落点

| 位置 | 改动 |
|---|---|
| `onNew(m)` | `if (!c) return` → `if (!canReadConv(m.conv)) return`（先判权限，再取会话对象） |
| `tick()` 的 `fresh.forEach` | 在 `onNew` 之前先过 `canReadConv`，非我的消息直接丢弃，不进 `S.recent` |
| `refreshAll()` | 重建白名单；`messages` 查询之后仍按白名单过滤一次再进 `S.recent` |
| `sendMsg()` | 插入成功后就地注册白名单 |
| `buildConvs()` | 渲染列表继续从关系数据推导（行为不变），但**不再承担隔离职责** |

### 为什么这次不会再翻车

上次失败是「白名单来源 = 已加载数据」+「消息是白名单的来源之一」形成循环，
新会话的第一条消息进不来。这次的白名单**明确包含发送即注册**这一路：
自己发的消息一定在，别人的消息则走关系数据 —— 只有关系数据尚未同步、
但会话是刚建好的极端情况才会短暂落在白名单外，而这种情况下
`buildConvs()` 本来就推不出该会话，**行为和现在完全一致**，不会比现状更差。

---

## 三、验收标准

1. `379 通过 / 0 失败` 基线不变（这一条是硬性的）
2. 新增 4 条针对性测试：
   - 陌生人私聊的消息不出现在侧栏、不计未读、不弹通知
   - 陌生人私聊的消息不进入 `S.recent`（防止它反过来污染会话推导）
   - 自己新建的会话，第一条消息能正常落位（回归上次的翻车点）
   - 群聊里非成员的会话消息被拦下
3. 桩 SDK 已具备 `in()` 等能力（已完成，`5d5ff9c`），可安全表达「只拉我参与的会话」

---

## 四、需要你确认的一点

**是否同时把 `messages` 的拉取收窄？** 有两种做法：

- **方案 A（保守）**：拉取不变（`select * where id > lastId`），只在客户端过滤。
  改动小、风险低，但**数据仍然会下传到浏览器**——陌生人私聊正文只是没显示，
  在内存和网络传输里依然存在。安全上只是「不显示」，不是「拿不到」。
- **方案 B（彻底）**：改为 `select * where conv in (...) and id > lastId`，
  只拉我参与的会话。这才是真正的隔离，且顺带减少流量。
  依赖刚补好的 `in()` 桩能力，需要先跑通测试。

我建议 **先做方案 A 落地（保证基线不动），紧接着在同一分支做方案 B**，
分两个提交，这样万一 B 有问题可以单独回退到 A。

要不要按这个方案推进？或者你倾向直接只做 B、跳过 A？
